cmake_minimum_required(VERSION 3.7.1)
project(smarttoolsEditor58)

set(CMAKE_MFC_FLAG 2)

add_definitions( -D_UNICODE -DUNICODE -DFMI_SUPPORT_STATION_DATA_SMARTTOOL)

include_directories( 
    ${LIBS_FMI}/smarttools/include
    ${LIBS_FMI}/newbase/include
    ${BOOST_INCLUDE_DIR}
    ${LIBS_FMI}/bizcode/stationviews/include
    ${LIBS_FMI}/bizcode/ctrlviews/include
    ${LIBS_FMI}/bizcode/generaldoc/include
    )

file( GLOB CPP_FILES source/*.cpp )
file( GLOB H_FILES include/*.h )

add_library( 
    ${PROJECT_NAME} STATIC
    ${CPP_FILES} 
    ${H_FILES} 
    )

